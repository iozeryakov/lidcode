import { FC } from "react";
import { Login } from "../../../components/Login";

/**
 * Компонент, отображающий страницу автоизации.
 * 
 * @returns {JSX.Element} - Компонент, отображающий страницу авторизации.
 */
export const AdminLogin: FC = () => {
  return (<Login />)
};
